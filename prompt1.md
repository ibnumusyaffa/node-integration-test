generate all acceptance case for checkout api world only

POST /checkout
body

```json
{
        products: [
          {
            id: productId,
            quantity: 2
          },
        ],
};
```

only for authenticated user
